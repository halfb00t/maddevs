/**
 * config of feature flags for pages
 * Need to pass an object with path and available environments
 * {
    path: '/',
    development: true,
    staging: true,
    production: false,
  },
 */
export const routerConfig = []
